export enum Folders {
	MONOPACKER = '.monopacker',
	INTERNAL = 'internal'
}

export enum Files {
	ANALYTICS = 'monopacker.analytics.json',
	REGISTRY = 'monopacker.registry.json',
	INSTALLER = 'monopacker.installer.js'
}
