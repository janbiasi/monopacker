export { Config, ResolverConfig, PackConfig, PackageManager } from './types';
export { getConfig } from './resolve';
export { validateConfig } from './validator';
export { defaultFilesToCopy, defaultIgnorePatterns } from './defaults';
